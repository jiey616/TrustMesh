name: trustmesh

services:
  mongo:
    image: mongodb/mongodb-community-server:7.0-ubuntu2204
    container_name: mongo
    restart: unless-stopped
    command: ["--bind_ip_all"]
    ports:
      - "27017:27017"
    volumes:
      - trustmesh-mongo-data:/data/db

  backend:
    build:
      context: ./backend
    container_name: trustmesh-backend
    restart: unless-stopped
    # 有界停机落盘：45s > 30s 回写预算 + 8s HTTP drain，避免编排层在落盘完成前
    # SIGKILL，那会导致停机期间的内存写入必丢。
    stop_grace_period: 45s
    depends_on:
      - mongo
      - qdrant
    environment:
      PORT: ${PORT:-8080}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      ALLOW_ALL_CORS: ${ALLOW_ALL_CORS:-false}
      JWT_SECRET: ${JWT_SECRET:?set JWT_SECRET in the project root .env file}
      ACCESS_TOKEN_TTL: ${ACCESS_TOKEN_TTL:-15m}
      REFRESH_TOKEN_TTL: ${REFRESH_TOKEN_TTL:-168h}
      MONGO_ENABLED: ${MONGO_ENABLED:-true}
      MONGO_URI: ${MONGO_URI:-mongodb://mongo:27017}
      MONGO_DATABASE: ${MONGO_DATABASE:-trustmesh}
      MONGO_TIMEOUT: ${MONGO_TIMEOUT:-5s}
      # 运维智能体巡检（默认开，可用 .env 覆盖关闭）
      OPS_ENABLED: ${OPS_ENABLED:-true}
      OPS_SCAN_INTERVAL: ${OPS_SCAN_INTERVAL:-5m}
      CLAWSYNAPSE_API_URL: ${CLAWSYNAPSE_API_URL:-http://trustmesh-clawsynapse:18080}
      # 节点本地 API Bearer token（clawsynapse v1.0.36+ requireBearer）。
      # 取自转发节点 DataDir 的 api_token 文件，配置在仓库根 .env。
      CLAWSYNAPSE_API_TOKEN: ${CLAWSYNAPSE_API_TOKEN:-}
      CLAWSYNAPSE_TIMEOUT: ${CLAWSYNAPSE_TIMEOUT:-3s}
      CLAWSYNAPSE_PEER_SYNC_INTERVAL: ${CLAWSYNAPSE_PEER_SYNC_INTERVAL:-10s}
      EMBEDDING_PROVIDER: ${EMBEDDING_PROVIDER:-openai}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      EMBEDDING_API_KEY: ${EMBEDDING_API_KEY:-}
      EMBEDDING_API_URL: ${EMBEDDING_API_URL:-https://api.openai.com/v1}
      EMBEDDING_DIMENSION: ${EMBEDDING_DIMENSION:-1536}
      QDRANT_URL: ${QDRANT_URL:-http://qdrant:6333}
      KNOWLEDGE_STORAGE_PATH: ${KNOWLEDGE_STORAGE_PATH:-/var/lib/trustmesh-knowledge}
      FILES_STORAGE_PATH: ${FILES_STORAGE_PATH:-/var/lib/trustmesh-files}
      # 默认值给 Docker Desktop 的宿主机网关（换环境也能用）；真实部署请在 .env 覆盖为
      # agent 容器确实能访问到的地址。切勿写死某台机器的局域网 IP（历史上写死过
      # 192.168.0.100，实际是 .101，导致 agent 下载全部 Connection refused）。
      TRUSTMESH_EXTERNAL_URL: ${TRUSTMESH_EXTERNAL_URL:-http://host.docker.internal:8080}
      DOWNLOAD_TOKEN_TTL: ${DOWNLOAD_TOKEN_TTL:-24h}
      WRITE_TIMEOUT: ${WRITE_TIMEOUT:-0}
      ASSISTANT_API_URL: ${ASSISTANT_API_URL:-https://api.openai.com/v1}
      PLATFORM_NAME: ${PLATFORM_NAME:-TrustMesh}
      ASSISTANT_API_KEY: ${ASSISTANT_API_KEY:-}
      ASSISTANT_MODEL: ${ASSISTANT_MODEL:-gpt-4o-mini}
    ports:
      - "8080:8080"
    volumes:
      - trustmesh-transfer-data:/var/lib/trustmesh-transfers:ro
      - trustmesh-knowledge-data:/var/lib/trustmesh-knowledge
      - trustmesh-files-data:/var/lib/trustmesh-files
      # 岗位市场静态数据（角色包 md + 索引），只读挂载
      - ./data:/app/data:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/healthz >/dev/null || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 10s

  clawsynapse:
    image: clawsynapse:local-rt4
    container_name: trustmesh-clawsynapse
    # 指定 container_name 后，compose 不再把 service 名注册成 DNS 别名。
    # 2026-09-09 起 CLAWSYNAPSE_API_URL 默认直接引用 container_name
    # （同网络下 Docker DNS 恒注册 container_name，不依赖别名）——
    # 节点容器脱离本 compose 重建（如升级 v1.0.36）时不再丢别名断连。
    networks:
      default:
        aliases:
          - clawsynapse
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      NODE_ID: ${CLAWSYNAPSE_NODE_ID:-trustmesh-server}
      NATS_SERVERS: ${NATS_SERVERS:-nats://175.27.135.91:4222}
      LOCAL_API_ADDR: ${CLAWSYNAPSE_LOCAL_API_ADDR:-0.0.0.0:18080}
      TRUST_MODE: ${CLAWSYNAPSE_TRUST_MODE:-open}
      AGENT_ADAPTER: ${CLAWSYNAPSE_AGENT_ADAPTER:-webhook}
      WEBHOOK_URL: ${CLAWSYNAPSE_WEBHOOK_URL:-http://backend:8080/webhook/clawsynapse}
      DATA_DIR: ${CLAWSYNAPSE_DATA_DIR:-/var/lib/clawsynapse}
      IDENTITY_KEY_PATH: ${CLAWSYNAPSE_IDENTITY_KEY_PATH:-/var/lib/clawsynapse/identity.key}
      IDENTITY_PUB_PATH: ${CLAWSYNAPSE_IDENTITY_PUB_PATH:-/var/lib/clawsynapse/identity.pub}
      DELIVERABLE_PREFIXES: ${DELIVERABLE_PREFIXES:-chat,task,todo,conversation,knowledge}
      TRANSFER_DIR: ${TRANSFER_DIR:-/var/lib/trustmesh-transfers}
      TRANSFER_TTL: ${TRANSFER_TTL:-24h}
      LOG_LEVEL: ${CLAWSYNAPSE_LOG_LEVEL:-info}
      LOG_FORMAT: ${CLAWSYNAPSE_LOG_FORMAT:-json}
    ports:
      - "18080:18080"
    volumes:
      - trustmesh-clawsynapse-data:/var/lib/clawsynapse
      - trustmesh-transfer-data:/var/lib/trustmesh-transfers
      - ./skills:/skills
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:18080/v1/health >/dev/null || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 10s

  qdrant:
    image: qdrant/qdrant:latest
    container_name: trustmesh-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - trustmesh-qdrant-data:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/127.0.0.1/6333' || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 5s

  frontend-v2:
    build:
      context: ./frontend-v2
    container_name: trustmesh-frontend-v2
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "5174:80"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1/ >/dev/null || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 5s

volumes:
  trustmesh-mongo-data:
  trustmesh-clawsynapse-data:
  trustmesh-transfer-data:
  trustmesh-qdrant-data:
  trustmesh-knowledge-data:
  trustmesh-files-data:
