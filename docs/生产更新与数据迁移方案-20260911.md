# 生产环境更新 + 数据迁移方案

> 编制时间：2026-09-11 12:40
> 生产：`175.27.135.91:62000`（cloudUser + `vic_key.pem`，免密 sudo）——**已连通**
> 状态：**待你确认后执行**

---

## 一、连通性结论（已解决）

| 项 | 结论 |
|---|---|
| SSH 用户 | **`cloudUser`**（此前试的 root/ubuntu/jiey/tm 全错） |
| 认证方式 | **公钥 `vic_key.pem`**（RSA 2048，指纹 `SHA256:v6nXfBq7MBRRp/5tgQaBUTULvVJNnzpJLAKj5kJQvY4`） |
| 密码认证 | ❌ 服务端已禁用（`allowed types: ['publickey']`）。`deploy_test.py:6` 里的密码 `uW7s-5zix.A3N5_` **已失效** |
| 权限 | `uid=1000(cloudUser) groups=...,991(docker)`，**`sudo -n` 免密可用** |
| 主机 | `VM-0-11-centos`，CentOS 7，kernel 3.10，x86_64，4C / 7.6G / 99G（用 57G，剩 39G） |
| ⚠️ 副作用 | 为让 ssh 接受该 key，我改了 `vic_key.pem` 的 ACL（原为 `Authenticated Users:Modify`，过宽被 ssh 拒）。现仅当前用户可读 |

---

## 二、生产现状（实测）

### 2.1 运行服务（compose 位于 `/opt/trustmesh-test/`）

```
trustmesh-backend        trustmesh/backend:test      Up 8d (healthy)   8080
trustmesh-frontend       trustmesh/frontend:test     Up 8d (healthy)   3000
trustmesh-frontend-v2    trustmesh/frontend-v2:test  Up 8d (healthy)   5174
trustmesh-clawsynapse    trustmesh/clawsynapse:test  Up 8d (healthy)   18080
trustmesh-mongo          mongo:7.0-ubuntu2204        Up 5w             27017
trustmesh-qdrant         qdrant:latest               Up 5w (unhealthy)
trustmesh-nats           nats:latest                 Up 5w             4222
trustmesh-nginx-proxy    nginx:1.27-alpine           Up 8d (healthy)   80/443
film-mcp-mcp-1           film-mcp-mcp                Up 3w             58080   ← 无关业务，勿动
film-mcp-pg-1            pgvector/pgvector:pg16      Up 5w (healthy)   5432    ← 无关业务，勿动
```

### 2.2 生产数据（**独立库，与本地无交集**）

| 集合 | 文档数 | 说明 |
|---|---|---|
| `projects` | **4** | 字圣仓颉项目(active)、大夏超神学院(active)、大夏超神学院(archived)、V2API-Smoke-Project(active) |
| `users` | **2** | `huazong_02@163.com`（画宗AIGC无人工厂）、`smoke-test-agent` |
| `agents` | 14 | 执行总控/导演/编剧/资产/美术/分镜/音频/视频/后期/制片/QA + 2 个 smoke |
| `meetings` | **18** | 字圣仓颉剧本讨论/围读共 14 场 + 大夏超神学院 4 场 |
| `tasks` | 30 | 字圣仓颉 11、大夏超神(archived) 7、**未归属 6**、大夏超神(active) 5、Smoke 1 |
| `artifacts` | 172 | |
| `comments` | 206 | |
| `events` | 689 | |
| `project_files` | 200 | |
| `agent_chats` | 28 · `meeting_messages` 413 · `notifications` 78 · `join_requests` 13 | |
| 磁盘文件 | `/var/lib/trustmesh-files` = **3.4M** | |

> 🔴 **生产有真实业务数据**（字圣仓颉 11 任务 + 14 场会议、大夏超神学院），**不能整体覆盖**。

### 2.3 版本差距

| 组件 | 生产 | 本地 |
|---|---|---|
| backend | 镜像建于 **2026-09-02**（8 天前） | 含 P-01/02/03/04/05/08 + 绑定交付物 API |
| frontend-v2 | bundle `index-iDP4IAdJ.js` | `index-DE4NlkQR.js`（含浅色主题修复 + 绑定 UI + AIGC 示例） |
| frontend | 8 天前 | 含聊天附件对齐 |
| `.env` | 已有 `DOWNLOAD_TOKEN_TTL=24h` ✅ | — |

---

## 三、本地数据（迁移源）

### 3.1 总量

| 集合 | 总量 | **5 个 active 项目** | 21 个 archived 项目 |
|---|---|---|---|
| `projects` | 26 | **5** | 21 |
| `tasks` | 179 | **38** | 141 |
| `project_files` | 340 | **125** | 215 |
| `comments` | 2054 | **1049** | 1005 |
| `events` | 5101 | **2019** | 3082 |
| `artifacts` | 368 | **0** ⚠️ | 368 |
| 磁盘 | 3.5M | ~2.5M | ~1M |

### 3.2 要迁移的 5 个 active 项目

| 项目 ID | 名称 | org | tasks | files | comments | events |
|---|---|---|---|---|---|---|
| `14341e53236507f1e0e6426b` | 西游记后转延续动画制作 | `org_6a9ad80564cc0dd9d78ce5b0` | 10 | 34 | 280 | 503 |
| `044ba61f38db97ee705c9c37` | 治愈烟火气微短剧制作 | 同上 | 9 | 24 | 190 | 345 |
| `c20a28793123895de89c08ec` | 影视制作项目 | 同上 | 5 | 20 | 279 | 520 |
| `99ec9b6984581d290a6ad49d` | 军旅影视制作 | 同上 | 3 | 20 | 87 | 277 |
| `57b802f746ee3c169d091712` | 测试项目全流程 | 同上 | 11 | 27 | 213 | 374 |

> ⚠️ **5 个项目的 tasks 全部 `archived` 状态归 0** —— 说明 38 个 task 关联的 artifacts 用 `project_id` 查不到（artifacts 靠 `task_id` 关联）。迁移时必须按 **task_id 集合**捞 artifacts，不能按 project_id。

### 3.3 本地非业务数据（**不迁移**）

- 11 个 users 中只有 `test001@163.com` 是常用账号，其余（debug/probe/e2e/test2/test3/sso/stage1/测试02）都是测试残留
- 12 个 organizations 中只有 `画宗`(?) 与 `test001@163.com` 是真实；`org_6a9ad80564cc0dd9d78ce5b0` 是真租户
- 21 个 archived 项目：`pentest-iso-project`×6、`sse-cross-a-*`×5、`阶段2-1 归属探针`、`阶段1探针` 等 —— 均为测试残留

---

## 四、迁移策略（三选一）

### 🅰 方案 A：**合并**（推荐）—— 保留生产业务数据 + 迁入本地 5 个 active 项目

- **保留**：生产全部现有数据（字圣仓颉、大夏超神学院、用户、agents、会议）
- **注入**：本地 5 个 active 项目 + 其 tasks/files/comments/events/artifacts + 磁盘文件
- **用户/组织处理**：ID 不冲突（本地 `06bcdb5a...` vs 生产 `6fc7170c...`），**直接插入**，两套账号并存
- ✅ 优点：零丢失，生产业务不中断
- ⚠️ 缺点：生产会出现 3 个用户、两套 organization；`huazong_02@163.com` 看不到本地迁入的项目（org 不同）
- 适合：**把本地 5 个项目作为"新客户/演示数据"推到生产**

### 🅱 方案 B：**定向覆盖** —— 删生产测试残留 + 迁入本地 5 个 active

- **删除**：生产 `V2API-Smoke-Project` + 2 个 smoke agent + `smoke-test-agent` 用户（纯测试残留）
- **保留**：字圣仓颉、大夏超神学院（含 archived）
- **注入**：本地 5 个 active 项目
- ✅ 优点：清掉生产垃圾
- 适合：**方案 A + 顺手清垃圾**

### 🅲 方案 C：**全量替换** —— 生产库清空，仅留本地 5 个 active 项目

- 🔴 **会删除生产"字圣仓颉"（11 任务 + 14 场会议）与"大夏超神学院"**
- 适合：**生产就是测试环境、数据无所谓**（`deploy_test.py` 里确实标 `test`）

> 🔴 需你明确选择。**默认我按 A 准备**（最安全，不删任何东西）。

---

## 五、执行步骤（以方案 A 为例，全程可回滚）

### 阶段 0：备份（**必做，不可跳过**）

| 步骤 | 内容 | 位置 |
|---|---|---|
| 0.1 | 生产 Mongo `mongodump` 全库 | 服务器 `/opt/trustmesh-test/backup-<ts>/` |
| 0.2 | 生产 files 卷 tar | 同上 |
| 0.3 | 生产 `.env` + `docker-compose.yml` 备份 | 同上（已有 `.bak` 惯例） |
| 0.4 | 生产镜像打 `test-prev-<ts>` 标签 | 服务器本地（已有惯例） |
| 0.5 | 本地 Mongo `mongodump` 全库 | 本地 `backups/` |

### 阶段 1：构建并推送新镜像

| 步骤 | 内容 |
|---|---|
| 1.1 | 本地 `docker-compose build backend frontend-v2 frontend`（linux/amd64） |
| 1.2 | `docker save` 为 tar（三镜像约 150MB） |
| 1.3 | SFTP 上传至服务器 `/tmp/` |
| 1.4 | 服务器 `docker load` + 打 `:test` 标签（旧镜像**保留** `test-prev-<ts>`） |

### 阶段 2：数据导出与注入

| 步骤 | 内容 |
|---|---|
| 2.1 | 本地按 **白名单** 导出：5 个 active 项目的 `projects`/`tasks`（按 project_id）/`project_files`/`comments`/`events`/`artifacts`（按 task_id）/`meetings`/`meeting_messages`/`agent_chats`/`notifications`；外加对应 `users`/`organizations`/`org_memberships`/`agents`/`join_requests` |
| 2.2 | `mongodump` 生成 BSON → `mongorestore --nsFrom=trustmesh.* --nsTo=trustmesh.*`（**insert 模式，不 drop**） |
| 2.3 | 磁盘文件：打包本地 `trustmesh-files-data` 中 5 个项目目录 → 解到生产卷对应路径 |
| 2.4 | 重启 backend（内存状态机需从 Mongo 重建） |

### 阶段 3：验证

| 步骤 | 内容 |
|---|---|
| 3.1 | 生产 `mongosh` 复核：projects=4+5、tasks=30+38、artifacts=172+38 项目内产物 |
| 3.2 | 生产 Web 冒烟：登录 `huazong_02@163.com` → 原项目可见；新项目列表可见 |
| 3.3 | 磁盘文件可下载（抽 3 个跨项目文件） |
| 3.4 | 硬刷新 `Ctrl+Shift+R` 确认新前端 bundle（`index-DE4NlkQR.js` 特征） |

---

## 六、风险与对策

| # | 风险 | 等级 | 对策 |
|---|---|---|---|
| R1 | **artifacts 按 task_id 而非 project_id 关联** | 🔴 高 | 导出时先捞 5 项目的 task_id 集合，再 `{task_id:{$in:[...]}}` 查 artifacts（本地已验证 active 项目 project_id 查得 0） |
| R2 | **`_id` 冲突** | 中 | 两库 ID 已比对：projects/users/agents 全部不重叠；仍会在注入前做一次交集校验 |
| R3 | **Store 内存态与 Mongo 不一致** | 高 | 注入后**必须重启 backend**（内存状态机 `loadState` 重建） |
| R4 | **磁盘文件名编码** | 中 | 中文文件名多，用 tar + `--strip` 而非逐文件 SFTP |
| R5 | **生产库被误写** | 高 | 阶段 1 全程只读；注入前先 `mongodump` 备份；用 `--nsFrom/--nsTo` 精确控制 |
| R6 | **`film-mcp` 被误伤** | 低 | 只操作 `trustmesh-*` 容器与 `trustmesh` 库，不碰 `film-mcp-*` |
| R7 | **`vic_key.pem` ACL 被我改过** | 低 | 已记录；如需还原可告知 |
| R8 | 4C/7.6G 内存紧张（已用 2.8G，可用 4.3G） | 中 | 镜像 load + 容器重建期间错峰；不并行跑重任务 |
| R9 | 生产 `qdrant` 已 unhealthy 5 周 | 低 | 既有问题，本次不引入；可顺带重启 |

---

## 七、待你确认

1. **策略选 A / B / C？**（默认 A：合并，不删任何生产数据）
2. **是否顺带升级前端 v1（`frontend/`）？** 还是只升 backend + frontend-v2？
3. **迁移范围**：只迁 5 个 active 项目的**业务数据**，还是也带上 `users`/`organizations`/`agents`（本地 agents 只有 7 个，生产已有 14 个）？
4. **是否同时把本地代码推到 GitHub**（`main` 领先 80 提交）？

> 确认后我按「备份 → 构建 → 上传 → 注入 → 验证」执行，每步做完向你报结果，不跳步。
